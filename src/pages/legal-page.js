// BATAILLE — pages d'information / légales
// Comportement identique pour les 6 pages (à propos, contact, FAQ,
// règles, confidentialité, conditions) : header + footer, rien de plus.
import { mountHeader } from '../components/header.js';
import { mountFooter } from '../components/footer.js';

mountHeader();
mountFooter();
